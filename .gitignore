# Project notes (kept local, not exposed via Pages)
CLAUDE.md

# Old project iteration (Rx telehealth pivot, not deployed)
archive/

# Heavy video backups — not referenced anywhere in production
assets/video/hero-fluid-orig.mp4
assets/video/skin-transform-orig.mp4

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
.claude/
*.swp
*.swo

# Archive zips (deployment artifacts kept local)
*.zip

# Local one-shot helper scripts (private — may handle tokens)
kakao-review.pdf
_kakao-review-build.py
_verify.py
_verify-pdf.py
_cf-cleanup.py

# Logs / env
*.log
.env
.env.local
.env.*.local

# Dependencies / build output
node_modules/
.next/
dist/
build/
